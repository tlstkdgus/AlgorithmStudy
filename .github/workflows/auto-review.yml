name: AI Code Review

on:
  push:
    paths:
      - "신상현/202**/**/*.py"
      - "신상현/202**/**/*.java"
      - "신상현/202**/**/*.cpp"
      - "신상현/202**/**/*.js"
      - "김효정/202**/**/*.py"
      - "김효정/202**/**/*.java"
      - "김효정/202**/**/*.cpp"
      - "김효정/202**/**/*.js"
      - "이원준/202**/**/*.py"
      - "이원준/202**/**/*.java"
      - "이원준/202**/**/*.cpp"
      - "이원준/202**/**/*.js"

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install google-genai

      - name: Get changed files
        id: changed-files
        run: |
          CHANGED_FILES=$(git -c core.quotepath=false diff --name-only HEAD~1 HEAD | grep -E '^(신상현|김효정|이원준)/[0-9]{6}/[0-9]{2}/.*\.(py|java|cpp|js)$' || true)
          echo "files<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGED_FILES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "Changed files: $CHANGED_FILES"

      - name: Run AI Review
        if: steps.changed-files.outputs.files != ''
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          echo "${{ steps.changed-files.outputs.files }}" | while read file; do
            if [ -n "$file" ]; then
              echo "Reviewing $file..."
              python scripts/all.py "$file" || echo "Failed to review $file"
            fi
          done

      - name: Commit review results
        if: steps.changed-files.outputs.files != ''
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add 신상현/202**/**/*_review.md 신상현/202**/**/*_optimized.md \
                  김효정/202**/**/*_review.md 김효정/202**/**/*_optimized.md \
                  이원준/202**/**/*_review.md 이원준/202**/**/*_optimized.md || true
          git diff --staged --quiet || git commit -m "🤖 AI Review: Auto-generated code review"

      - name: Push changes
        if: steps.changed-files.outputs.files != ''
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
